# tinflate

Distributed under a BSD-3-Clause license.

The original (untouched) version lives as `tinflate.c.orig`.  Because the code assumes that the source was correct, the presh version has the decompression error checks removed.