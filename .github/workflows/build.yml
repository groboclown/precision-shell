name: Build
on:
  # Trigger the workflow on pushes to only the 'master' branch (this avoids duplicate checks being run e.g. for dependabot pull requests)
  push:
    branches: [main, dev]
  # Trigger the workflow on any pull request
  pull_request:

jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.properties.outputs.version }}
      changelog: ${{ steps.properties.outputs.changelog }}
    steps:

      # Check out current repository
      - name: Fetch Sources
        uses: actions/checkout@v3
      
      # Install tools
      - name: Install Required tools
        run: |
          sudo apt-get update
          sudo apt-get install podman
      
      - name: Test
        run: |
          podman build -f test.Dockerfile .
      
      - name: dietlibc
        run: |
          podman build -f build-dietlibc.Dockerfile -t dietlibc .
          ./extract-executables.sh dietlibc -o out/dietlibc
          ls -lA out/dietlibc
      - name: glibc
        run: |
          podman build -f build-glibc.Dockerfile -t glibc .
          ./extract-executables.sh glibc -o out/glibc
          ls -lA out/glibc
      - name: musl
        run: |
          podman build -f build-musl.Dockerfile -t musl .
          ./extract-executables.sh musl -o out/musl
          ls -lA out/musl
